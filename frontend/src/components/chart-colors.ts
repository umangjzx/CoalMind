/**
 * CoalMind AI — categorical chart palette.
 * Kept in its own module (no component exports) so Vite fast-refresh stays happy.
 * Values resolve from the CSS custom properties in src/styles/index.css.
 */
export const KIND_COLOR = [
  "rgb(var(--k-1))",
  "rgb(var(--k-2))",
  "rgb(var(--k-3))",
  "rgb(var(--k-4))",
  "rgb(var(--k-5))",
  "rgb(var(--k-6))",
  "rgb(var(--k-7))",
];
